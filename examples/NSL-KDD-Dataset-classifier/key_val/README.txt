README.txt

This directory(key_val) contains the key-value pair.
KDD-CUP99 dataset contains string data within it. As per now, we are not able to process
it with the standard machine learning models.
Hence we are trying to convert it to appropriate integer values.

This is just a temporary fix. We are trying to make it more robust
by making it deal with real strings.